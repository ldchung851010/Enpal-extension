{
  "name": "enpal-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'",
    "test:unit": "node --test 'tests/unit/*.test.js'"
  }
}
